namespace Core.Object.Module
{
    /// <summary>
    /// The rank of a module, which determines its stats.
    /// </summary>
    public enum ModuleRank
    {
        Alpha = 0,
        Beta,
        Gamma,
        Delta,
        Epsilon,
        Zeta,
        Eta,
        Theta,
        Iota,
        Kappa,
        Lambda,
    }
}
